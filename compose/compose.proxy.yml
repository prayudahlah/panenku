services:
  nginx:
    image: nginx:alpine
    container_name: panenku-proxy
    ports: ["0.0.0.0:${NGINX_PORT}:80"]
    volumes:
      - ../docker/nginx/default.conf:/etc/nginx/templates/default.conf.template
      - ../frontend/dist:/usr/share/nginx/html
    environment:
      BACKEND_URL: ${PROXY_BACKEND}
      NGINX_RESOLVER: 100.100.100.100
    dns:
      - 100.100.100.100
    command:
      - sh
      - -c
      - |
        envsubst '$$BACKEND_URL $$NGINX_RESOLVER' \
          < /etc/nginx/templates/default.conf.template \
          > /etc/nginx/conf.d/default.conf \
        && nginx -g 'daemon off;'
